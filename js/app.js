'use strict';

//Object 1 naming seattleStore

var seattleStore = {
    name: 'Seattle',
    hoursOpen: 14,
    avgCookieSale: 6.3,
    minCustomer: 23,
    maxCustomer: 65,
    bananas: 
    function storeVistors(){{for (var i = 0; i < 14; i++);{
    function bananastwo(min, max)
    return Math.floor(Math.random() * (max - min + 1))+ min;
     }
}
    console.log (seattleStore.bananas(seattleStore.minCustomer, seattleStore.maxCustomer))



//Global Functions
// Customer Count Generator

//for (var i = 0; i < 14; i++){

//function getRandomCustomers(min,max){
  //  min = Math.ceil(min);
    //max = Math.floor(max);
    //return Math.floor(Math.random()* (max-min+1)+min);
//} 
//CustomersPerHour.push(getRandomCustomers(seattleStore.minCustomer,seattleStore.maxCustomer))}


// Customer Count Tracker Array

//customersPerHour = []
//console.log(customersPerHour[])



   